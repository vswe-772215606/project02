// Intentionally minimal. We do not use Electron IPC for business operations
// per the architecture decision in 00-shared/decisions.md (Kitchen Display UI talks
// to the backend over HTTP, not IPC).
